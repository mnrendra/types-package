/**
 * Controls the Markdown rendering engine used in the Marketplace. Either
 * github (default) or standard.
 *
 * note: This is not a `package.json` official field.
 */
export type Markdown = 'github' | 'standard'
